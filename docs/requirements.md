# izicrm — Требования

Персональный Telegram-бот для учёта балансов банковских карт и расчёта P&L.
Валюта: **только RUB**. Мультивалютность вне scope.

Документ — нормализованный источник требований. Каждое требование имеет ID и трассируется
в тесты (`docs/financial-model.md` §10) и код.

Статус: **архитектурный этап**. Реализация не начата.

---

## 1. Глоссарий

| Термин | Определение |
|---|---|
| **Карта** (Card) | Именованный счёт пользователя. Не хранит номер карты, только название. |
| **Баланс** (Balance) | Значение денег на карте на конкретную **бизнес-дату**. |
| **Бизнес-дата** | Календарная дата в таймзоне пользователя (`Europe/Moscow` по умолчанию). Не UTC-дата. |
| **Запись баланса** (Balance entry) | Неизменяемый факт: (карта, бизнес-дата, сумма, источник). |
| **Актуальная запись** | Единственная не-вытесненная (`superseded_at IS NULL`) запись на (карта, дата). |
| **LOCF** | Last Observation Carried Forward — баланс на дату D = последняя актуальная запись с `effective_date ≤ D`. |
| **Капитал** (Capital) | Сумма LOCF-балансов всех карт, находящихся в scope на дату D. |
| **In scope на D** | `created_on ≤ D AND (archived_on IS NULL OR D < archived_on)`. |
| **Внешний поток** (Capital flow) | Ввод/вывод денег из системы учёта. Не является P&L. |
| **Депозит** (Deposit) | Внешний ввод: начальный баланс новой карты с типом `BALANCE`. |
| **Вывод** (Withdrawal) | Внешний вывод: остаток карты, архивированной с причиной `WITHDRAWN`. |
| **P&L** | Изменение капитала, очищенное от внешних потоков. Единственная мера прибыли. |
| **Изменение баланса карты** | Дельта по одной карте. **Информационная метрика, не прибыль** (см. FR-5.4). |

---

## 2. Функциональные требования

### FR-1. Пользователи и изоляция

| ID | Требование | Источник |
|---|---|---|
| FR-1.1 | Пользователь идентифицируется по `telegram_id`. Регистрация неявная при первом `/start`. | §2 |
| FR-1.2 | Данные пользователей строго изолированы. Пользователь A никогда не видит данные B. | §13 |
| FR-1.3 | Любой запрос к пользовательской сущности обязан включать `user_id`. Методы вида `getCard(cardId)` запрещены; только `getUserCard(userId, cardId)`. | §13 |
| FR-1.4 | Изоляция обеспечивается на двух уровнях: обязательный `user_id` в API репозиториев **и** PostgreSQL Row Level Security. | §13 |
| FR-1.5 | Telegram UI не является уровнем безопасности. Любой `card_id` из `callback_data` считается недоверенным и перепроверяется по `user_id`. | §13 |

### FR-2. Карты

| ID | Требование | Источник |
|---|---|---|
| FR-2.1 | Создание карты: пользователь вводит название и текущий баланс. | §3 |
| FR-2.2 | После ввода баланса бот спрашивает тип: `[📊 Просто текущий баланс]` / `[📈 Это прибыль]`. | §3 |
| FR-2.3 | Тип `BALANCE`: начальный баланс — точка отсчёта, **не** прибыль. Создаёт депозит. | §3, §6 |
| FR-2.4 | Тип `PROFIT`: начальный баланс учитывается как прибыль. Депозит **не** создаётся. | §3, §6 |
| FR-2.5 | Название уникально среди **активных** карт пользователя (см. C-6). Сравнение регистронезависимое, с нормализацией пробелов. | §3, §11 |
| FR-2.6 | Переименование карты. Уникальность проверяется заново. | §3, §11 |
| FR-2.7 | Архивирование карты. Физическое удаление финансовых данных запрещено. | §1, §4 |
| FR-2.8 | Архивная карта: не входит в текущий капитал, не показывается среди активных, сохраняется в истории, **участвует в отчётах за периоды, когда была активна**. | §4 |
| FR-2.9 | При архивировании карты с ненулевым остатком требуется явное указание судьбы остатка: вывод или перевод на другую карту (см. C-3). | производное от §1, §7 |
| FR-2.10 | Список карт с текущим балансом и изменением баланса. | §11 |
| FR-2.11 | Разархивирование в v1 **не поддерживается** (см. C-9). | производное |

### FR-3. История балансов

| ID | Требование | Источник |
|---|---|---|
| FR-3.1 | Сохраняется каждый ежедневный баланс каждой карты. | §4 |
| FR-3.2 | Возможность восстановить состояние: «какой баланс был у каждой карты в дату D» (через LOCF). | §4 |
| FR-3.3 | История не удаляется при архивировании карты. | §4 |
| FR-3.4 | Записи истории неизменяемы (append-only). Исправление создаёт новую запись и логически вытесняет предыдущую; старая строка сохраняется. | §4, §14 |
| FR-3.5 | На (карта, бизнес-дата) существует не более одной актуальной записи. Гарантируется частичным unique-индексом в БД. | производное |

### FR-4. Обновление балансов

| ID | Требование | Источник |
|---|---|---|
| FR-4.1 | Кнопка `[🔄 Обновить баланс]` запускает пошаговый диалог. | §8 |
| FR-4.2 | Для каждой карты бот показывает название и предыдущий баланс, затем принимает новое значение. | §8 |
| FR-4.3 | Пользователь может обновить одну карту или все карты. | §8 |
| FR-4.4 | Режим «все карты»: последовательный проход по активным картам, затем итог. | §8 |
| FR-4.5 | Ввод карты вручную не требуется — выбор через inline-кнопки + state machine. | §8 |
| FR-4.6 | Бизнес-дата фиксируется в момент **старта** диалога и применяется ко всем картам этого прохода (см. C-11). | производное |
| FR-4.7 | Повторный ввод баланса за ту же дату — это исправление: новая актуальная запись, старая вытесняется. | производное от FR-3.4 |

### FR-5. Расчёты

| ID | Требование | Источник |
|---|---|---|
| FR-5.1 | **Основной P&L считается от общего капитала пользователя.** Запрещено считать основной P&L как сумму P&L отдельных карт. | §1, §5 |
| FR-5.2 | Перемещение денег между своими картами при неизменном капитале даёт P&L = 0. | §1 |
| FR-5.3 | Количество карт не влияет на P&L. Создание и архивирование карт P&L-нейтральны. | §6, §7 |
| FR-5.4 | Изменение баланса отдельной карты показывается как информационная метрика. Сумма таких изменений в общем случае ≠ P&L пользователя. | §10, §1 |
| FR-5.5 | Дневной P&L: `Capital(D) − Capital(prev) − NetFlow(prev, D]`. Формула в `docs/financial-model.md`. | §5, C-1 |
| FR-5.6 | Если предыдущей записи нет — P&L не рассчитывается, показывается `—`. | §5 |
| FR-5.7 | Если база процента ≤ 0 — процент не определён, показывается `—`. Деление на ноль недопустимо. | C-2 |
| FR-5.8 | Месячный P&L: `Capital(E) − Capital(S−1) − NetFlow[S, E]`, где `S` — 1-е число месяца. База отсчёта — **закрытие последнего дня предыдущего месяца**, не открытие 1-го числа (см. C-4). | §7, C-4 |
| FR-5.9 | Единый источник истины: одна функция `periodPnl(from, to)`. Дневной, месячный и общий P&L — её параметризации. | §18 |
| FR-5.10 | Пропущенные дни: синтетические записи не создаются, нулевая прибыль не подставляется, предупреждений нет. P&L считается за фактический интервал между обновлениями. | §9 |
| FR-5.11 | Знак: положительный P&L с `+`, отрицательный с `−`. | §5 |

### FR-6. Telegram UI

| ID | Требование | Источник |
|---|---|---|
| FR-6.1 | Главный экран: блок месяца, блок последнего дня, список активных карт, кнопки. Макет в `docs/telegram-flows.md`. | §10 |
| FR-6.2 | Кнопки: `[🔄 Обновить баланс]`, `[💳 Карты]`, `[➕ Добавить карту]`, `[⚙️ Управление картами]`, `[📊 Excel-отчёт]`. | §10 |
| FR-6.3 | Handlers не содержат бизнес-логику: только Telegram-события, кнопки, ввод, состояния. | §16 |
| FR-6.4 | Все многошаговые сценарии реализованы явной state machine, не каскадами `if/else`. | §17 |
| FR-6.5 | Любой шаг диалога имеет `[Отмена]`; невалидный ввод не продвигает состояние. | производное |

### FR-7. Excel-отчёт

| ID | Требование | Источник |
|---|---|---|
| FR-7.1 | Кнопка `[📊 Excel-отчёт]` формирует файл со статистикой за всё время из данных PostgreSQL. | §12 |
| FR-7.2 | Листы: Сводка, Динамика, Балансы карт, Карты, Месяцы, Движения капитала, Инфо. Спецификация в `docs/excel-report.md`. | §12 |
| FR-7.3 | Графики: общий капитал по времени, дневной P&L, месячная прибыль/доходность. | §12 |
| FR-7.4 | Отчёт содержит данные **только запросившего пользователя**. Проверяется тестом. | §13 |

---

## 3. Нефункциональные требования

| ID | Требование | Источник |
|---|---|---|
| NFR-1 | PostgreSQL, одна физическая БД, логическая изоляция по `user_id`. Отдельная БД на пользователя запрещена. | §14 |
| NFR-2 | Все денежные значения — `NUMERIC(20,2)`. Floating point для денег запрещён на всех уровнях. | §14, §15 |
| NFR-3 | Все изменения схемы — только через миграции. Нужны FK, индексы, unique constraints. | §14 |
| NFR-4 | Финансовые расчёты детерминированы: `Decimal`, явный режим округления, никаких `number`/`Math.round`/`parseFloat`/`toFixed`, никаких неявных `Decimal → number`. | §15 |
| NFR-5 | Каждая финансовая формула покрыта unit-тестами. Обязательный набор сценариев — `docs/financial-model.md` §10. | §15 |
| NFR-6 | Слои: Telegram handlers → application services → financial domain → repositories → PostgreSQL. Обратные зависимости запрещены. | §16 |
| NFR-7 | Код: типизированный, модульный, тестируемый, читаемый, production-oriented. | §18 |
| NFR-8 | Финансовая логика не дублируется. Один источник истины для P&L. | §18 |
| NFR-9 | Каждый значимый финансовый метод: понятное имя, docstring с формулой, unit-тесты. | §18 |
| NFR-10 | Row Level Security как дополнительный уровень защиты, а не замена скоупингу в коде. | §13 |
| NFR-11 | Все денежные вычисления выполняются в domain-слое на `Decimal`. Арифметика над деньгами в SQL (включая `SUM`) запрещена. | производное от NFR-8 |

---

## 4. Реестр противоречий и неполноты требований

Каждая позиция: **проблема → почему это ошибка → принятое решение**.
Пункты, менявшие или расширявшие исходные требования, помечены статусом решения.
Сводка — в §6 «Журнал решений». Блокирующих вопросов не осталось.

---

### C-1. §5 даёт ложную прибыль при добавлении карты — прямое противоречие с §6

**Проблема.** §5 определяет `DailyProfit = TotalToday − TotalPrevious`, где `TotalPrevious` —
сумма предыдущих балансов активных карт. Карта, созданная сегодня, предыдущего баланса не имеет.
При трактовке «нет записи → 0» добавление карты на 30 000 ₽ даёт `DailyProfit = +30 000 ₽`.

§6 требует прямо противоположного: начальный баланс новой карты не является доходом.

**Почему это ошибка.** Формула §5 не содержит члена для внешних потоков, но §6 требует его наличия.
Формулы §5 и §6 несовместимы.

**Решение.** Дневная формула получает тот же член внешних потоков, что и месячная:

```
DailyPnL(D) = Capital(D) − Capital(prev) − NetFlow(prev, D]
```

Дневной и месячный P&L становятся частными случаями одной функции `periodPnl(from, to)` (FR-5.9).

---

### C-2. Деление на ноль в формуле процента

**Проблема.** `DailyProfitPercent = DailyProfit / TotalPrevious * 100`. Требования не описывают
`TotalPrevious = 0`. Реально достижимо: первая карта создана с типом `PROFIT` (база = 0);
все карты в нуле; все карты архивированы.

**Почему это ошибка.** Деление на ноль. При `TotalPrevious = 0` и `DailyProfit > 0` доходность
математически не определена (бесконечна), а не равна нулю.

**Решение.** Процент определён только при `base > 0`. Иначе выводится `—`. Реализуется как
`PercentResult = { defined: true, value: Decimal } | { defined: false, reason: 'ZERO_BASE' | 'NO_PREVIOUS' }`.
Тип запрещает «случайно» получить число. Отрицательная база (кредитная карта в минусе) также даёт `—`.

---

### C-3. Архивирование карты создаёт фантомный P&L — требования не покрывают этот случай **[РЕШЕНО: расширение принято]**

**Проблема.** §4 говорит, что архивная карта не участвует в текущем капитале, но не говорит, что
происходит с P&L в день архивирования. Капитал падает на остаток карты.

Разбор случаев (вчера: Сбер1 = 10 000, Сбер2 = 20 000, капитал = 30 000):

| Сценарий | Без обработки потока | Корректно |
|---|---|---|
| Архив Сбер2, деньги сняты в банкомате | P&L = −20 000 (ложный убыток) | P&L = 0 |
| Перевод 20 000 на Сбер1 (Сбер1 = 30 000), затем архив Сбер2 без обнуления | P&L = +20 000 (ложная прибыль) | P&L = 0 |

**Почему это ошибка.** Нарушается FR-5.3 («количество карт не влияет на P&L») и §7
(«P&L отражает реальное изменение капитала, а не изменение количества карт»).
Отличить вывод от внутреннего перевода по данным невозможно — информации физически нет.

**Решение.** Архивирование карты с ненулевым остатком требует одного уточняющего вопроса:

- `[💸 Деньги выведены]` → `WITHDRAWAL` на сумму остатка. Капитал ↓, P&L = 0.
- `[↔️ Переведены на другую карту]` → выбор карты-получателя; в одной транзакции остаток
  прибавляется к балансу получателя за ту же бизнес-дату, поток не создаётся. Капитал не меняется, P&L = 0.

При нулевом остатке вопрос не задаётся.

**Обоснование расширения.** Это минимальный механизм, без которого заявленный в §1 и §7 инвариант
недостижим ни при каком порядке действий пользователя. Логика выведена из требований, не придумана.

**Статус: подтверждено заказчиком 20.08.2026.** Расширение требований принято; вариант с двумя
кнопками реализуется как описано выше.

**Теорема (следствие).** Внешний поток нужен ровно тогда, когда карта входит в активное множество
или выходит из него. Пока обе карты активны, перевод между ними автоматически даёт P&L = 0, потому
что капитал не изменился — специальная операция «перевод» между активными картами не нужна.

---

### C-4. «Относительно начала месяца» — двусмысленно и в одной трактовке теряет прибыль за 1-е число

**Проблема.** §7: «месячная статистика считается относительно начала месяца». Два чтения:
`Capital(1-е число)` или `Capital(последний день предыдущего месяца)`.

**Почему это ошибка.** Если взять `Capital(1 августа)`, а пользователь обновил балансы 1 августа,
то прибыль за 1 августа попадает в базу отсчёта и **исчезает** из августовского P&L. Месячные P&L
перестают складываться в общий P&L.

**Решение.** Базой служит закрытие предыдущего дня: `OpeningCapital(M) = Capital(S − 1 день)`.
Это делает периоды непересекающимися и сохраняет аддитивность (`docs/financial-model.md` §6).

---

### C-5. Макет §10 внутренне противоречив по округлению процентов

**Проблема.** Проверка чисел макета:

| Строка макета | Восстановленная база | Точный процент | HALF_UP | Обрезание |
|---|---|---|---|---|
| `СЕГОДНЯ +1 200 ₽ (+1.04%)` | 114 800 | 1.045296…% | **1.05%** | **1.04%** |
| `Сбер2 +700 ₽ (+3.4%)` | 20 600 | 3.398058…% | **3.4%** | **3.3%** |

Значение «сегодня» требует обрезания, значение «Сбер2» требует округления. Одновременно оба
недостижимы. Дополнительно: `10 500 + 21 300 + 7 800 = 39 600 ≠ 116 000` — список карт не сходится
с показанным капиталом; разное число знаков (`5.0%` против `1.04%`).

**Решение.** Единое правило: проценты округляются `ROUND_HALF_UP` до **2 знаков**, деньги — до
копеек, `.00` при отображении опускается. По этому правилу корректное значение в макете — `+1.05%`.
Инвариант «Σ балансов активных карт = показанный капитал» закрепляется тестом.

**Набор метрик доходности (подтверждено 20.08.2026).** В Telegram выводится только простая
доходность `PnL / OpeningCapital`, как в требованиях. В Excel к ней добавляются Modified Dietz
и time-weighted return — они расходятся с простой ровно тогда, когда в периоде были внешние
потоки, и это расхождение само является полезной информацией.

---

### C-6. §3 и §11 задают разную область уникальности названия

**Проблема.** §3: «уникальным в пределах одного пользователя». §11: «не допускать две **активные**
карты с одинаковым названием».

**Решение (подтверждено).** Принимается более узкая формулировка §11: частичный unique-индекс
`(user_id, name_norm) WHERE archived_on IS NULL`. Название архивированной карты можно переиспользовать.
Дополнительно нормализация: `trim`, сжатие внутренних пробелов, регистронезависимое сравнение —
чтобы `«Сбер1»`, `«сбер1»` и `«Сбер1 »` не были разными картами. Отчёты различают одноимённые карты по ID.

---

### C-7. §2 закрывает модель: реальное пополнение будет учтено как прибыль

**Проблема.** §2 запрещает спрашивать, является ли изменение прибылью или пополнением; §6 вводит
внешний поток при создании карты. Следовательно, единственные внешние потоки в системе — создание и
архивирование карты. Приход зарплаты на карту будет учтён как прибыль.

**Решение.** Это принятое и осознанное ограничение, зафиксированное как аксиома модели
(`docs/financial-model.md` §2, аксиома A-3), а не дефект. Отдельные операции пополнения/снятия
вне scope v1. Ограничение выводится в лист «Инфо» Excel-отчёта, чтобы пользователь понимал природу цифр.

---

### C-8. Не определена таймзона и граница суток **[принято по умолчанию, меняется конфигурацией]**

**Проблема.** «Раз в день» — по какому времени? При UTC обновление в 01:00 МСК попадёт в предыдущую дату.

**Решение.** Бизнес-дата вычисляется в таймзоне пользователя; по умолчанию `Europe/Moscow`
(нет переходов на летнее время, соответствует RUB). Поле `users.tz` заложено в схему.
Дополнительно хранится `recorded_at TIMESTAMPTZ` для аудита. Все даты в БД — `DATE` (бизнес-дата),
все моменты времени — `TIMESTAMPTZ`.

---

### C-9. Не определены разархивирование и история переименований

**Решение (v1).**
- Разархивирование не поддерживается: обратная операция потребовала бы депозита на восстановленный
  остаток и усложнила бы модель потоков. Пользователь создаёт новую карту.
- Переименование не версионируется: отчёты показывают текущее название. Идентичность карты — `card_id`,
  поэтому история не рвётся. Ведётся `audit_log` для восстановления цепочки переименований.

---

### C-10. Не определён формат ввода суммы

**Решение.** Принимаются: цифры, пробелы/узкие пробелы/подчёркивания как разделители разрядов,
`,` или `.` как десятичный разделитель, не более 2 знаков после разделителя, опциональный ведущий `−`.
Отклоняются: выражения, `1e5`, `10k`, третий знак после запятой (**с ошибкой, без молчаливого
округления** — см. C-12), значения вне `±10^15`. Относительный ввод (`+500`) не поддерживается:
он противоречит семантике «введите текущий баланс».

---

### C-11. Диалог, начатый до полуночи, может «размазаться» по двум датам

**Проблема.** Проход «обновить все карты», начатый 31 августа в 23:59, завершится 1 сентября.
Часть карт попадёт в август, часть в сентябрь — оба месячных P&L будут искажены.

**Решение.** Бизнес-дата вычисляется один раз при старте диалога, хранится в состоянии FSM
(`dialog_states.business_date`) и применяется ко всем шагам прохода (FR-4.6).

---

### C-12. `NUMERIC(20,2)` неявно округляет — это нарушает NFR-4

**Проблема.** PostgreSQL при вставке `10000.005` в `NUMERIC(20,2)` молча округлит до `10000.01`.
Это ровно то неявное округление, которое запрещено §15.

**Решение.** Валидация на входе отклоняет более 2 знаков после запятой с понятным сообщением.
БД остаётся вторым барьером. Драйвер `pg` возвращает `NUMERIC` как строку — запрещено регистрировать
type parser, превращающий его в `number`; на это есть отдельный тест.

---

### C-13. «Удалить» карту противоречит запрету физического удаления

**Проблема.** §1 говорит «удалять/архивировать», §4 запрещает физическое удаление финансовых данных.

**Решение.** Операция удаления отсутствует. В UI кнопка называется «Архивировать» с пояснением
«история сохранится». `ON DELETE RESTRICT` на всех финансовых FK как технический барьер.

---

### C-14. Блок «сегодня» вводит в заблуждение, если сегодня обновлений не было

**Проблема.** При LOCF `Capital(сегодня) = Capital(последнего обновления)`, поэтому блок «СЕГОДНЯ»
покажет `+0 ₽ (0.00%)`, хотя пользователь просто не обновлял балансы. §9 при этом запрещает
предупреждать о пропущенных днях.

**Решение.** Блок озаглавливается фактической датой последнего обновления: `СЕГОДНЯ · 20 августа`,
если обновление было сегодня, иначе `ПОСЛЕДНЕЕ ОБНОВЛЕНИЕ · 16 августа`. При интервале более
одного дня добавляется `за N дней`. Предупреждений нет — только честная подпись. Бизнес-логика не меняется.

---

### C-15. Не определена база доходности за всё время

**Проблема.** Простая доходность `P&L / OpeningCapital` для периода «за всё время» имеет базу 0
(до первой карты капитала не было) — не определена.

**Решение.** За всё время база — сумма депозитов (реально внесённые деньги):
`TotalReturn = TotalPnL / TotalDeposits`. Если депозитов нет (все карты созданы как `PROFIT`) — `—`.
Дополнительно в Excel выводится Modified Dietz как взвешенная по времени альтернатива.

---

### C-16. Проценты не складываются — это нужно явно зафиксировать

**Проблема.** Рублёвые P&L аддитивны (телескопирование), проценты — нет: сумма дневных процентов
≠ месячный процент.

**Решение.** Аддитивность рублей закрепляется property-тестом. Проценты никогда не суммируются.
В Excel рядом с простой доходностью выводится time-weighted return `Π(1 + rᵢ) − 1` как корректная
composed-метрика.

---

## 5. Вне scope v1

Мультивалютность · операции пополнения/снятия вне создания и архивирования карты (C-7) ·
разархивирование (C-9) · относительный ввод сумм (C-10) · автоматическая интеграция с банками ·
несколько Telegram-аккаунтов на одного пользователя · шаринг данных между пользователями ·
прогнозирование и налоговая отчётность.

---

## 6. Журнал решений

| ID | Вопрос | Решение | Дата |
|---|---|---|---|
| C-3 | Судьба остатка при архивировании карты | Два варианта: «Деньги выведены» / «Переведены на другую карту» | 20.08.2026 |
| C-5 | Набор метрик доходности | Простая в Telegram; Modified Dietz и TWR дополнительно в Excel | 20.08.2026 |
| ADR-006 | Библиотека генерации Excel | `@office-kit/xlsx` — нативные графики, точная фиксация версии | 20.08.2026 |
| C-8 | Таймзона бизнес-даты | `Europe/Moscow` по умолчанию, поле `users.tz` заложено в схему | принято по умолчанию |

Открытых блокирующих решений нет — архитектурный этап закрыт.
